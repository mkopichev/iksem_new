package com.iksem.fragmentsCommunicator;

public enum FragmentsList {
    NO_FRAGMENT,
    PROTOCOLS_FRAGMENT,
    MAP_FRAGMENT,
    HOME_FRAGMENT,
    BLUETOOTH_FRAGMENT,
    SETTING_FRAGMENT,
    CALIBRATION_FRAGMENT
}
